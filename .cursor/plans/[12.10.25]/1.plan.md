# SVM Implementation - Task 1 Exam

**Date**: 12.10.25  
**Objective**: Implement Task 1 from README.md with documentation for learning

---

## CONTEXT IN(@ui/pages/svm.py, @docs/5-SVM-Kernel.ipynb, @README.md) OUT(understanding_map)

```
project: {
  stack: ["Python", "Streamlit", "scikit-learn", "numpy"],
  structure: "Streamlit single-page app with tabs",
  current_state: "UI exists but needs documentation and experiment tracking"
}

rules: [@parlang-guide.md, @user_rules]

current_state: {
  svm.py: "Has working train functionality with CV, metrics, visualizations",
  notebook: "Contains theory and math explanations about kernels",
  missing: [
    "Documentation section for learning",
    "Experiment tracking to compare multiple runs",
    "Parameter explanations (WHY we use C, gamma, etc.)"
  ]
}
```

---

## OBJECTIVES as obj OUT(task_list)

### obj["documentation_section"]
```
why: "User needs to understand kernel theory, math, and WHY things work to complete exam analysis"
DoD: [
  "Expandable documentation section at top of SVM tab",
  "Theory about non-linear mappings and kernel trick",
  "Math formulas rendered with LaTeX",
  "Explanation of each kernel type (Linear, RBF, Poly, Sigmoid)",
  "Parameter explanations (C, gamma, degree) with examples"
]
```

### obj["experiment_tracking"]
```
why: "Exam requires testing 'different kernels and parameters' and comparing results"
DoD: [
  "Store all training experiments in session_state",
  "Display table with experiment history (kernel, params, metrics)",
  "Allow user to compare multiple runs side-by-side",
  "Clear experiment history button"
]
```

### obj["parameter_explanations"]
```
why: "User needs to know WHAT each parameter does and WHEN to use it"
DoD: [
  "Enhanced tooltips on parameter controls",
  "Visual guide showing parameter effects",
  "Recommendations for parameter ranges"
]
```

### obj["enhanced_visualization"]
```
why: "User needs to analyze results and draw conclusions for exam"
DoD: [
  "Keep existing confusion matrix and metrics bars",
  "Add comparison chart for multiple experiments",
  "Add kernel performance comparison"
]
```

---

## DEPENDENCIES

```
SEQUENCE: [
  "1. obj['documentation_section'] - FIRST (user emphasized learning)",
  "2. obj['parameter_explanations'] - helps understand controls",
  "3. obj['experiment_tracking'] - core functionality",
  "4. obj['enhanced_visualization'] - final analysis"
]

REQUIREMENTS: {
  tools: ["streamlit", "sklearn.svm.SVC", "matplotlib", "numpy"],
  files: [
    "@ui/pages/svm.py (modify)",
    "@docs/5-SVM-Kernel.ipynb (extract theory)"
  ]
}
```

---

## CONSTRAINTS

```
- Files must be < 300 lines (split if needed)
- Use existing project patterns
- Don't break existing functionality
- Follow user rules (no trailing semicolons, clear explanations)
```

---

## VALIDATION_CRITERIA

```
ACCEPTANCE: [
  "✅ Documentation section displays with theory and math",
  "✅ User can read about kernel trick and understand WHY",
  "✅ Training still works (no breaking changes)",
  "✅ Experiments are tracked and can be compared",
  "✅ User can identify best kernel/params from table",
  "✅ LaTeX math renders correctly",
  "✅ Code stays under 300 lines per file"
]
```

---

## NOTES

User confusion about "http://localhost:8501/svm" route:
- Streamlit tabs don't create separate routes by default
- Solution: Add documentation IN the tab itself (better UX)
- User can study theory while using interactive interface

