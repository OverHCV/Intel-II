# Interactive Streamlit UI for Bank Marketing ML Analysis

## CONTEXT IN(@config.py, @settings/, @funcs/, @README.md) OUT(environment_map) {

project: ["Python", "Streamlit", "Scikit-learn", "Matplotlib", "Seaborn"],
current_state: [
"@config.py - Centralized configuration with all hyperparameters",
"@settings/feats.py - Feature and target definitions",
"@settings/options.py - Enum-like option classes",
"@settings/imports.py - All library imports",
"@funcs/evaluator.py - K-fold cross-validation implementation",
"@data/bank.csv - Bank Marketing dataset (4521 samples)",
"@README.md - Three tasks: SVM (0.9pts), ANN (0.9pts), PCA (0.7pts)"
],
rules: [
"Files must be < 300 lines (single responsibility)",
"No trailing semicolons in Python",
"Use config.CONF for all parameters via Keys",
"Modular architecture (separate logic from UI). Make as many UI files as needed so all is modular, like a react-application",
"Windows PowerShell environment with UV package manager"
],
why: "Create interactive Streamlit UI instead of static notebooks for better presentation and exploration"
}

## OBJECTIVES as obj OUT(goal_list) [

"streamlit_setup": {
why: "Need Streamlit installed and configured in project",
DoD: "Streamlit added to pyproject.toml and can run basic app"
},

"ui_architecture": {
why: "Need organized structure for UI components, visualizations, and logic",
DoD: "Folder structure created: ui/components/, ui/pages/, ui/utils/, funcs/visualizers.py"
},

"base_layout": {
why: "Need main app with tab navigation and two-column layout pattern",
DoD: "Main Streamlit app with 3 tabs (SVM, ANN, PCA) and sidebar controls"
},

"data_loader": {
why: "Need centralized data loading and preprocessing for all tabs",
DoD: "Function that loads, encodes categoricals, scales features, returns X, y"
},

"svm_interactive_tab": {
why: "Task 1 requires testing different SVM kernels and parameters",
DoD: "Interactive tab with sliders for C, gamma, degree; kernel selector; real-time accuracy plot; confusion matrix; CV scores display"
},

"ann_interactive_tab": {
why: "Task 2 requires testing different ANN architectures and activations",
DoD: "Interactive tab with architecture selector, activation dropdown, solver selector; loss curve plot; accuracy display; confusion matrix"
},

"pca_interactive_tab": {
why: "Task 3 requires PCA analysis and comparison with original models",
DoD: "Interactive tab with n_components slider; variance explained plot; side-by-side comparison of best SVM/ANN before/after PCA"
},

"visualization_functions": {
why: "Need reusable plotting functions for confusion matrices, learning curves, scatter plots",
DoD: "funcs/visualizers.py with at least: plot_confusion_matrix, plot_learning_curve, plot_pca_variance, plot_decision_boundary"
},

"working_prototype": {
why: "Need to validate the approach works before building all features",
DoD: "Minimal working app: loads data, shows one interactive plot with slider, updates in real-time"
}

]

## DEPENDENCIES [

obj["streamlit_setup"] → obj["ui_architecture"],
obj["ui_architecture"] → obj["base_layout"],
obj["base_layout"] → obj["working_prototype"],
obj["data_loader"] → obj["svm_interactive_tab"],
obj["data_loader"] → obj["ann_interactive_tab"],
obj["data_loader"] → obj["pca_interactive_tab"],
obj["visualization_functions"] → obj["svm_interactive_tab"],
obj["visualization_functions"] → obj["ann_interactive_tab"],
obj["visualization_functions"] → obj["pca_interactive_tab"],
obj["working_prototype"] → obj["svm_interactive_tab"],
obj["svm_interactive_tab"] → obj["ann_interactive_tab"],
obj["ann_interactive_tab"] → obj["pca_interactive_tab"]
]

## TECHNICAL_DECISIONS {

layout_strategy: {
decision: "Two-column layout with st.columns([2, 1])",
left_column: "Visualizations (plots, confusion matrices)",
right_column: "Interactive controls (sliders, selectors, metrics)",
why: "User specified left=graph, right=sliders. 2:1 ratio gives good balance"
},

state_management: {
decision: "Use st.session_state for trained models and results",
why: "Avoid retraining on every slider movement; cache expensive computations"
},

real_time_updates: {
decision: "Train on button click, not on every slider change",
why: "SVM/ANN training is expensive; user controls when to retrain"
},

data_preprocessing: {
decision: "OneHotEncode categoricals, StandardScale numericals",
why: "Required for SVM/ANN; configured in config.CONF[Keys.HANDLE_CATEGORICAL]"
},

visualization_library: {
decision: "Matplotlib for plots, render with st.pyplot()",
why: "Already in config imports; familiar; works well with Streamlit"
}

}

## ARCHITECTURE OUT(folder_structure) {

ui/ {
app.py: "Main Streamlit entry point with tab navigation",
components/: {     
    # Like in react, I know is kinda different in streamlit but if possible make a tab component
    # (so we can change the selected exercise), panels so one can be right, other in the left, a slider thing
    # like the basic things you would make in react right? So yeah those below are usefull but kinda chunky in my opinion
    # are like big blocks you arrive after created smaller component ones.
sidebar.py: "Shared sidebar components",
metrics_display.py: "Reusable metric cards",
model_controls.py: "Parameter sliders and selectors"
},
pages/: {
svm.py: "SVM interactive tab content", # don't like that `_page` preffix, if is into pages clearly is a page
ann.py: "ANN interactive tab content",
pca.py: "PCA interactive tab content"
},
utils/: {
    data_loader.py: "Load and preprocess data", # Yeah, I think also a tab for general config or change data (small dataset/large dataset) would be nice, an extra first tab
    state_manager.py: "Manage st.session_state"
}
},

funcs/ {
visualizers.py: "Plotting functions for all visualizations",
evaluator.py: "Already exists - k-fold CV implementation"
}

}

## INITIAL_QUESTIONS as questions [

questions["data_preprocessing"]: {
ASK: "Should we use the small dataset (4.5K) or full dataset (45K) for the interactive UI?",
options: [
"1. Small dataset (faster, more responsive for real-time interaction)",
"2. Full dataset (more accurate, better for final results)",
"3. Let user choose via UI toggle"
],
recommendation: "Option 1 for prototype, Option 3 for final version",
why: "Affects training speed and UI responsiveness"
},

> Yeah as I've said earlier let's make it selectable (3 option), worst case just a toglable button

questions["categorical_encoding"]: {
ASK: "How to handle the 9 categorical features?",
options: [
"1. OneHotEncode (creates many features, ~50+ columns)",
"2. LabelEncode (ordinal, may mislead models)",
"3. Use only numerical features (7 features, simpler)"
],
current_config: "config.CONF[Keys.HANDLE_CATEGORICAL] = 'onehot'",
recommendation: "Option 1 (OneHot) as configured, but show feature count in UI",
why: "Affects model performance and feature space size"
},

> Could be also a selectable, but first let's just arrive with value, a result, so (3) could be
> But make a way that's possible for later on to work as selectable.

questions["cv_strategy"]: {
ASK: "Use K-Fold CV on every parameter change or just hold-out validation?",
options: [
"1. K-Fold CV every time (slow but robust, 5-10 seconds per train)",
"2. Simple train/test split (fast, ~1 second)",
"3. K-Fold only on 'Evaluate' button click"
],
recommendation: "Option 3 - fast train/test for interactive exploration, CV on demand",
why: "Balance between accuracy and responsiveness"
}

]

> Hmm really don't even understand question, look, this can be a selectable param on that zero-tab we want to add
> So I can select there if is working with a simple-train so it goes fast, or selecting k-fold every time and that would take those 5-10 mentioned seconds
> Also, as that number of folds is changable that would be a better idea, so I can move a discrete-slider from 1 to 10 so I can choose fast to slower

## STATUS_POINTER {

current: "PLANNING PHASE - Awaiting user answers to questions",
next: "obj['streamlit_setup']: CREATE: pyproject.toml modification"
}

I've already installed streamlit so you can start rn using it!