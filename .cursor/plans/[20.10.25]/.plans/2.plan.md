# Plan 2: SVM Interface Refinement & Code Modularization

## CONTEXT IN(@README.md, @svm/tab.py, @parlang-guide.md) OUT(environment_map) {
  project: "Streamlit ML Analysis - Bank Marketing Dataset",
  current_issues: [
    "tab.py has 439 lines (needs modularization)",
    "Model Performance graphs stacked vertically (should be side-by-side)",
    "Model Configuration visible in all tabs (should hide in non-performance tabs)",
    "No user guide in svm/ folder for exam requirements"
  ],
  constraints: [
    "Must complete Task 1 requirements from README.md",
    "Keep existing functionality intact",
    "Follow ParLang best practices (modular, focused files)"
  ]
}

## OBJECTIVES as obj OUT(goal_list) [
  "verify_task1_complete": {
    why: "Ensure all requirements from README.md Task 1 are met",
    DoD: "Checklist shows all items completed, ready for exam submission"
  },
  
  "fix_graph_layout": {
    why: "Confusion matrix and metrics bar stacked vertically makes page too long",
    DoD: "Both graphs displayed side-by-side in Model Performance tab"
  },
  
  "hide_config_in_other_tabs": {
    why: "Model Configuration section takes space in Feature Analysis and Data Exploration tabs where it's not needed",
    DoD: "Configuration expander only visible in Model Performance tab"
  },
  
  "create_user_guide": {
    why: "Need comprehensive guide in svm/ folder explaining how to complete Task 1 for exam",
    DoD: "README.md in svm/ folder with step-by-step instructions, requirements checklist"
  },
  
  "modularize_tab_file": {
    why: "tab.py has 439 lines - violates single responsibility principle, hard to maintain",
    DoD: "tab.py < 150 lines, logic extracted to focused modules"
  }
]

## DEPENDENCIES [
  obj["verify_task1_complete"] BEFORE obj["create_user_guide"]
    reason: "Need to know what's missing before writing guide",
  
  obj["modularize_tab_file"] PARALLEL_WITH obj["fix_graph_layout"]
    reason: "Can refactor code structure while fixing layout",
  
  obj["hide_config_in_other_tabs"] AFTER obj["fix_graph_layout"]
    reason: "Layout changes affect where config should appear"
]

## ANALYSIS {
  
  UNDERSTAND IN(@README.md:Task1) OUT(requirements_checklist): {
    required: [
      "✅ Train SVM with cross-validation - DONE",
      "✅ Test different kernels (Linear, RBF, Poly, Sigmoid) - DONE", 
      "✅ Test different kernel parameters (C, gamma, degree) - DONE",
      "✅ Experiment tracking and comparison - DONE",
      "✅ Performance metrics (accuracy, precision, recall, F1) - DONE",
      "✅ Confusion matrix visualization - DONE",
      "✅ Save best model - DONE"
    ],
    optional_enhancements: [
      "✅ Correlation heatmap - DONE",
      "✅ Interactive scatter plots - DONE",
      "✅ Persistent experiment history - DONE"
    ],
    conclusion: "Task 1 is COMPLETE! All requirements met + extras"
  }
  
  UNDERSTAND IN(@svm/tab.py:structure) OUT(current_structure): {
    total_lines: 439,
    breakdown: {
      imports: "lines 1-24 (24 lines)",
      main_function: "lines 27-439 (412 lines!) ← PROBLEM",
      sections: [
        "Data loading: 36-40 (5 lines)",
        "Documentation: 42-43 (2 lines)",
        "Model Configuration: 48-216 (168 lines) ← Extract",
        "Visualizations: 219-350 (131 lines) ← Extract", 
        "Experiment History: 354-355 (2 lines - already extracted)",
        "Save Best Model: 357-438 (81 lines) ← Extract"
      ]
    },
    issues: [
      "All logic in single function",
      "Mixing concerns (config, training, viz, model saving)",
      "Hard to test individual components",
      "Violates Single Responsibility Principle"
    ]
  }
  
  TRACE IN(@svm/tab.py:line48) OUT(config_visibility_flow): {
    current: "Expander created at line 48, always visible",
    problem: "Visible in all tabs but only relevant for Model Performance",
    impact: "Takes vertical space, confuses user in Feature Analysis/Data Exploration",
    solution: "Move config inside Model Performance tab OR use st.session_state to track active tab"
  }
  
  TRACE IN(@svm/tab.py:line240-256) OUT(graph_layout_flow): {
    current: `
      with viz_tabs[0]:
          st.markdown("### Confusion Matrix & Metrics")
          # Show graphs side by side
          col_cm, col_metrics = st.columns(2)  ← ALREADY SIDE BY SIDE!
    `,
    wait_what: "User says they're stacked but code shows columns(2)?",
    hypothesis: "Maybe user hasn't seen latest changes OR something overwrote the layout",
    need_to_verify: "Check actual file content at lines 240-256"
  }
}

## PROPOSED_ARCHITECTURE {
  
  modular_structure: {
    "svm/tab.py": {
      lines: "~100",
      role: "Main orchestrator - loads components and renders page",
      content: [
        "Imports",
        "Main svm_page() function",
        "Calls component functions",
        "Layout coordination"
      ]
    },
    
    "svm/components/": {
      "model_config.py": {
        lines: "~100",
        role: "Model configuration UI and training logic",
        functions: [
          "render_model_configuration()",
          "train_svm_model(X, y, params, cv_strategy)"
        ]
      },
      
      "visualizations.py": {
        lines: "~80",
        role: "Visualization tabs rendering",
        functions: [
          "render_model_performance_tab()",
          "render_feature_analysis_tab()",
          "render_data_exploration_tab()"
        ]
      },
      
      "best_model_saver.py": {
        lines: "~60",
        role: "Best model selection and saving",
        functions: [
          "render_best_model_saver()"
        ]
      }
    }
  }
  
  config_visibility_solution: {
    approach: "Conditional rendering based on selected viz tab",
    implementation: `
      # Only show config when not in visualization mode OR in performance tab
      if not st.session_state.svm["is_trained"]:
          render_model_configuration()  # Always show when no model trained
      else:
          # Track active tab
          viz_tab_names = ["Model Performance", "Feature Analysis", "Data Exploration"]
          selected_tab = st.session_state.get("svm_active_viz_tab", 0)
          
          if selected_tab == 0:  # Model Performance tab
              with st.sidebar:
                  render_model_configuration()  # Show in sidebar for Model Performance
          
          # Render viz tabs
          render_visualizations()
    `
  }
}

## VALIDATION_CRITERIA {
  obj["verify_task1_complete"]: [
    "✓ All required items from README.md Task 1 checked off",
    "✓ Can demonstrate: different kernels, parameters, CV, tracking",
    "✓ Documentation clearly states completion status"
  ],
  
  obj["fix_graph_layout"]: [
    "✓ Confusion matrix and metrics bar side-by-side in Model Performance",
    "✓ Page doesn't require excessive scrolling",
    "✓ Both graphs visible simultaneously"
  ],
  
  obj["hide_config_in_other_tabs"]: [
    "✓ Config visible in Model Performance tab",
    "✓ Config hidden in Feature Analysis tab",
    "✓ Config hidden in Data Exploration tab",
    "✓ Full width available for visualizations in non-performance tabs"
  ],
  
  obj["create_user_guide"]: [
    "✓ README.md created in svm/ folder",
    "✓ Explains all Task 1 requirements",
    "✓ Step-by-step instructions to complete task",
    "✓ Checklist for exam submission",
    "✓ Links to relevant interface sections"
  ],
  
  obj["modularize_tab_file"]: [
    "✓ tab.py has < 150 lines",
    "✓ Logic extracted to focused modules in svm/components/",
    "✓ Each module has single responsibility",
    "✓ No functionality broken",
    "✓ Code is more maintainable and testable"
  ]
}

## STATUS_POINTER {
  current: "PLANNING PHASE - analyzing requirements",
  next: "EXECUTION PHASE - verify graph layout first, then modularize"
}

