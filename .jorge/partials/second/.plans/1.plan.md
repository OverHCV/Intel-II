# Plan 1: SVM Interface Improvements

## CONTEXT IN(@svm/tab.py, @visualizers.py, @experiments.py, @README.md) OUT(environment_map) {
  project: "Streamlit ML Analysis Application - Bank Marketing Dataset",
  technology_stack: ["Python 3.x", "Streamlit", "scikit-learn", "matplotlib", "seaborn", "pandas"],
  current_state: {
    svm_page: "@.jorge/partials/second/ui/pages/svm/tab.py",
    visualizers: "@.jorge/partials/second/funcs/visualizers.py", 
    experiments: "@.jorge/partials/second/ui/pages/svm/experiments.py",
    components: "@.jorge/partials/second/ui/components/*.py",
    config: "@.jorge/partials/second/settings/config.py"
  },
  rules: ["@.cursor/parlang-guide.md"],
  constraints: [
    "Must maintain existing session_state structure",
    "Must not break ANN or PCA tabs",
    "Must be modular and reusable",
    "Follow existing code style and patterns"
  ]
}

## OBJECTIVES as obj OUT(goal_list) [
  "remove_redundant_metrics": {
    why: "Metrics are displayed twice (sidebar grid + bar chart in viz section), confusing and cluttering UI",
    DoD: "Only bar chart remains in visualization section, sidebar metrics removed, UI is cleaner"
  },
  
  "fix_metrics_calculation": {
    why: "Confusion matrix shows poor results (zeros in cells) but metrics show .88 accuracy - indicates wrong averaging for multiclass or binary class imbalance not handled properly",
    DoD: "Metrics accurately reflect confusion matrix, user can trust the scores displayed, proper averaging strategy used"
  },
  
  "persist_experiment_history": {
    why: "User loves experiment history table but it's lost on page refresh - needs to persist across sessions",
    DoD: "Experiment history persists across sessions (saved to JSON file), loads on startup, user can track experiments over time"
  },
  
  "clarify_best_model_save": {
    why: "Button says 'Save as Best Model' but it's unclear if it saves current model or best from history - user is confused",
    DoD: "Button automatically identifies and saves the best model from experiment history, button text is clear 'Save Best Model (Exp #X)', user understands what's being saved"
  },
  
  "add_correlation_heatmap": {
    why: "Only 2 graphs is limiting - user wants correlation heatmap to understand feature relationships",
    DoD: "New correlation heatmap visualization showing relationships between features, helps with feature analysis"
  },
  
  "add_scatter_plots": {
    why: "User wants interactive scatter plots (3D if possible, 2D otherwise) with configurable X/Y/Z axes for exploratory data analysis",
    DoD: "Scatter plot with dropdown selectors for axes (2D: X/Y or 3D: X/Y/Z if feasible), user can explore feature relationships visually"
  },
  
  "modularize_visualizations": {
    why: "Current viz system has only 2 hardcoded graphs - needs modular architecture to easily add more visualizations",
    DoD: "Visualization system refactored into tabs or expanders, easy to add new viz types, clean separation of concerns"
  }
]

## DEPENDENCIES [
  obj["fix_metrics_calculation"] BEFORE obj["remove_redundant_metrics"]
    reason: "Need to ensure metrics are correct before deciding which display to keep",
  
  obj["modularize_visualizations"] BEFORE obj["add_correlation_heatmap"]
    reason: "Need modular architecture in place before adding new visualizations",
  
  obj["modularize_visualizations"] BEFORE obj["add_scatter_plots"]
    reason: "Need modular architecture in place before adding new visualizations",
  
  obj["persist_experiment_history"] PARALLEL_WITH obj["fix_metrics_calculation"]
    reason: "Independent tasks, can be done simultaneously",
  
  obj["clarify_best_model_save"] AFTER obj["persist_experiment_history"]
    reason: "Best model logic needs to work with persisted history"
]

## ANALYSIS {
  
  UNDERSTAND IN(@svm/tab.py) OUT(current_flow): {
    metrics_display_flow: [
      "1. Train button clicked → model trains",
      "2. Metrics calculated (accuracy, precision, recall, f1)",
      "3. Stored in session_state.svm['metrics']",
      "4. Displayed in SIDEBAR (lines 196-204) via MetricsGrid",
      "5. Also displayed in VIZ SECTION (lines 223-229) via plot_metrics_bars",
      "→ REDUNDANT: Same data displayed twice"
    ],
    
    metrics_calculation_issue: [
      "Lines 127-131: precision_score/recall_score/f1_score with average='binary'",
      "Problem: 'binary' average assumes 2 classes, but what if data is imbalanced?",
      "Confusion matrix shows zeros → suggests one class dominates or metrics are wrong",
      "Need to check: Is data truly binary? Is averaging strategy correct?",
      "Should use: average='weighted' or average='macro' for imbalanced data"
    ],
    
    experiment_history_flow: [
      "Lines 177-190: Experiments appended to session_state.svm['experiment_history']",
      "render_experiment_history() displays table from this list",
      "Problem: session_state is cleared on page refresh → history lost",
      "Solution: Save to JSON file, load on init"
    ],
    
    best_model_save_flow: [
      "Lines 241-256: Button 'Save as Best Model'",
      "Currently saves: st.session_state.svm['model'] (current/last trained model)",
      "Problem: User wants BEST model from history, not current model",
      "Solution: Find best experiment from history, save that model",
      "Challenge: Models in history are not stored, only params/metrics"
    ]
  }
  
  TRACE IN(@svm/tab.py, @experiments.py, @visualizers.py) OUT(complete_flow): {
    from: "User clicks Train SVM",
    through: [
      "svm_page() → train button handler",
      "Model trained with SVC",
      "Metrics calculated: accuracy, precision, recall, f1",
      "Results stored in session_state.svm",
      "Experiment added to experiment_history list",
      "MetricsGrid displays metrics in sidebar",
      "plot_metrics_bars displays metrics in viz section",
      "plot_confusion_matrix displays confusion matrix",
      "render_experiment_history displays all experiments table"
    ],
    to: "Results displayed in UI (but not persisted to disk)",
    
    validates: [
      "Session state management works ✅",
      "Metrics calculation uses average='binary' (might be wrong) ⚠️",
      "No persistence layer exists ❌",
      "Current model saved as 'best', not actual best ❌"
    ]
  }
  
  IDENTIFY IN(@visualizers.py) OUT(available_visualizations): {
    current: [
      "plot_confusion_matrix() - Heatmap of confusion matrix",
      "plot_metrics_bars() - Bar chart of metrics",
      "plot_pca_variance() - PCA variance plots",
      "plot_learning_curve() - Learning curves",
      "plot_comparison() - Before/after comparison",
      "plot_decision_boundary_2d() - 2D decision boundary"
    ],
    missing: [
      "Correlation heatmap for features",
      "Interactive scatter plots (2D/3D)",
      "Feature importance plots",
      "ROC curves",
      "Precision-Recall curves"
    ],
    architecture: "All functions return matplotlib figures, called directly in tab.py"
  }
}

## QUESTIONS {
  
  ASK IN(metrics_calculation_issue) OUT(user_decision): human {
    question: "What is the target variable distribution in your dataset?",
    options: [
      "1. Binary balanced (roughly 50/50)",
      "2. Binary imbalanced (e.g., 90/10)",
      "3. Multiclass (more than 2 classes)"
    ],
    context: "This determines which averaging strategy to use for precision/recall/f1",
    required_for: obj["fix_metrics_calculation"],
    blocks_execution: false,
    why: "Need to choose correct averaging: 'binary', 'weighted', 'macro', or 'micro'",
    fallback: "Will investigate data_info to determine automatically"
  }
  
  ASK IN(best_model_save_flow) OUT(user_decision): human {
    question: "How should 'Save Best Model' work?",
    options: [
      "1. Retrain best experiment params on demand when saving",
      "2. Store actual model objects in experiment history (memory intensive)",
      "3. Only save params of best model (can't use for PCA without retraining)"
    ],
    context: "Currently models are not stored in history, only params and metrics",
    required_for: obj["clarify_best_model_save"],
    blocks_execution: false,
    why: "Need to decide architecture for model persistence",
    fallback: "Option 1 - retrain best params when 'Save Best Model' clicked"
  }
  
  ASK IN(add_scatter_plots) OUT(user_decision): human {
    question: "For 3D scatter plots, should we use plotly (interactive) or matplotlib (consistent with rest)?",
    options: [
      "1. Plotly - interactive 3D rotation, zoom, hover",
      "2. Matplotlib - consistent styling, but 3D is less interactive", 
      "3. Both - use plotly for 3D, matplotlib for 2D"
    ],
    context: "Current app uses matplotlib for all plots",
    required_for: obj["add_scatter_plots"],
    blocks_execution: false,
    why: "Matplotlib 3D plots are not very good, plotly is better for 3D",
    fallback: "Option 3 - use plotly for 3D scatter, matplotlib for rest"
  }
}

## PROPOSED_ARCHITECTURE {
  
  visualization_system: {
    current: "Flat structure - 2 plots hardcoded in col_viz",
    proposed: "Tab-based or expander-based modular system",
    structure: {
      viz_tabs: [
        "Model Performance": [
          "Confusion Matrix (keep)",
          "Metrics Bar Chart (keep)"
        ],
        "Feature Analysis": [
          "Correlation Heatmap (NEW)",
          "Scatter Plot Matrix (NEW)"
        ],
        "Data Exploration": [
          "Interactive Scatter (2D/3D) (NEW)"
        ]
      ]
    },
    benefits: [
      "Easy to add new visualizations",
      "Organized by purpose",
      "Doesn't clutter main view",
      "User can focus on what they need"
    ]
  },
  
  persistence_system: {
    storage: "JSON file: .jorge/partials/second/.cache/svm_experiments.json",
    structure: {
      experiments: [
        {
          id: "unique_id",
          timestamp: "ISO_datetime",
          params: {kernel, C, gamma, degree},
          metrics: {accuracy, precision, recall, f1},
          training_time: "float",
          cv_strategy: "train_test | kfold"
        }
      ]
    },
    operations: [
      "load_experiments() - on app init",
      "save_experiment() - after training",
      "clear_experiments() - user action",
      "get_best_experiment() - for saving best model"
    ]
  },
  
  metrics_calculation: {
    current: "average='binary' hardcoded",
    proposed: "Auto-detect from data or use 'weighted'",
    logic: `
      if len(classes) == 2:
          if is_balanced:
              average = 'binary'
          else:
              average = 'weighted'  # Handles imbalance
      else:
          average = 'weighted'  # Multiclass
    `
  }
}

## VALIDATION_CRITERIA {
  obj["remove_redundant_metrics"]: [
    "✓ Sidebar metrics section removed (lines 196-204)",
    "✓ Visualization section still has metrics bar chart",
    "✓ No duplicate data displayed",
    "✓ UI is cleaner and less confusing"
  ],
  
  obj["fix_metrics_calculation"]: [
    "✓ Metrics use appropriate averaging strategy",
    "✓ Confusion matrix and metrics are consistent",
    "✓ High accuracy score only when confusion matrix shows good distribution",
    "✓ Documentation explains which averaging is used and why"
  ],
  
  obj["persist_experiment_history"]: [
    "✓ Experiments saved to JSON file after each training",
    "✓ Experiments loaded from file on app startup",
    "✓ History persists across page refreshes and app restarts",
    "✓ Clear history button works",
    "✓ No errors if file doesn't exist (first run)"
  ],
  
  obj["clarify_best_model_save"]: [
    "✓ Button automatically finds best experiment by accuracy",
    "✓ Button label shows which experiment is best: 'Save Best Model (Exp #5)'",
    "✓ Clicking button retrains model with best params",
    "✓ Success message confirms which experiment was saved",
    "✓ No confusion about what's being saved"
  ],
  
  obj["add_correlation_heatmap"]: [
    "✓ New visualization function plot_correlation_heatmap() in visualizers.py",
    "✓ Displays correlation matrix of all features",
    "✓ Uses seaborn heatmap with annotations",
    "✓ Integrated into modular visualization system",
    "✓ Helps user understand feature relationships"
  ],
  
  obj["add_scatter_plots"]: [
    "✓ Interactive scatter plot component created",
    "✓ Dropdown selectors for X, Y (and Z if 3D) axes",
    "✓ User can choose any features for axes",
    "✓ Points colored by target class",
    "✓ 3D plot rotatable (if plotly), or 2D if matplotlib only",
    "✓ Integrated into modular visualization system"
  ],
  
  obj["modularize_visualizations"]: [
    "✓ Visualization section uses tabs or expanders",
    "✓ Organized by category: Model Performance, Feature Analysis, Data Exploration",
    "✓ Easy to add new visualizations without cluttering code",
    "✓ Each viz has its own function/component",
    "✓ No breaking changes to existing plots"
  ]
}

## RISKS_AND_MITIGATIONS {
  risk_1: {
    description: "Changing metrics calculation might change historical experiment scores",
    impact: "HIGH - user's experiment history becomes inconsistent",
    mitigation: [
      "Add 'metrics_version' field to experiments",
      "Recalculate old experiments on load if version mismatch",
      "Or show warning that metrics calculation changed"
    ]
  },
  
  risk_2: {
    description: "Adding plotly introduces new dependency",
    impact: "MEDIUM - need to update pyproject.toml, user needs to install",
    mitigation: [
      "Make plotly optional - fallback to matplotlib if not installed",
      "Or stick with matplotlib only (user said 2D is acceptable)"
    ]
  },
  
  risk_3: {
    description: "Retraining best model on save might take time",
    impact: "LOW - user might wonder why save button is slow",
    mitigation: [
      "Show spinner with message 'Retraining best model...'",
      "Cache last trained model params to avoid retrain if same"
    ]
  },
  
  risk_4: {
    description: "Correlation heatmap might be too large with many features",
    impact: "MEDIUM - plot becomes unreadable with 50+ features",
    mitigation: [
      "Allow user to select subset of features",
      "Or automatically select top N most important features",
      "Or use hierarchical clustering to group features"
    ]
  }
}

## STATUS_POINTER {
  current: "PLANNING PHASE - awaiting user confirmation",
  next: "EXECUTION PHASE - implement objectives in dependency order"
}

